// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource sqlite {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "sqlite"
}

generator client {
  provider = "prisma-client-js"
  output   = "node_modules/.prisma/client"
}

model Widget {
  id                     String       @id @default(cuid())
  canonicalId            String       @unique @default("")
  type                   String
  version                Int
  data                   String?
  from_PageWidget_widget PageWidget[] @relation("PageWidget_widget")
}

model PageWidget {
  id                String  @id @default(cuid())
  container_classes String  @default("")
  order             Int
  widget            Widget? @relation("PageWidget_widget", fields: [widgetId], references: [id])
  widgetId          String? @map("widget")

  @@index([widgetId])
}

model Page {
  id                      String          @id @default(cuid())
  canonicalId             String          @unique @default("")
  title                   String          @default("")
  version                 Int
  styles                  String          @default("")
  from_PublishedPage_page PublishedPage[] @relation("PublishedPage_page")
}

model PublishedPage {
  id          String    @id @default(cuid())
  url         String    @unique @default("")
  contentHash String    @default("")
  template    String
  publishTime DateTime?
  page        Page?     @relation("PublishedPage_page", fields: [pageId], references: [id])
  pageId      String?   @map("page")

  @@index([pageId])
}